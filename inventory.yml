all:
  hosts:
    decidim-server:
        ansible_host:
        git_organization:
        git_root:
        git_repository:
        repository_branch:
        decidim_host:
        certbot_email_address:
        database_name:
        secret_key_base:
        decidim_deployment_path: 
        decidim_home_path:
        ssl_certificate_path: 
        ssl_certificate_key_path: 
        decidim_admin_email: 